#!/bin/bash

# Platform detection wrapper for test script
# This script detects the platform and calls the appropriate test script

if [[ "$OSTYPE" == "msys" || "$OSTYPE" == "cygwin" || "$OSTYPE" == "win32" ]]; then
    # Windows (Git Bash, Cygwin, or MSYS)
    exec bin/windows/test.bat "$@"
elif [[ "$OSTYPE" == "darwin"* ]]; then
    # macOS
    exec bin/unix/test.sh "$@"
elif [[ "$OSTYPE" == "linux-gnu"* ]]; then
    # Linux
    exec bin/unix/test.sh "$@"
else
    # Default to Unix for other platforms
    exec bin/unix/test.sh "$@"
fi
