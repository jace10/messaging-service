#!/bin/bash

# Database clearing script for messaging service
# Supports Unix-like systems (Linux, macOS)

set -e

# Get the directory where this script is located
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Run the Unix database clear script
exec "$SCRIPT_DIR/unix/db-clear.sh" "$@"
