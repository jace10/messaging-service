#!/bin/bash

# Check dependencies script for messaging service
# Supports Unix-like systems (Linux, macOS)

if [[ "$OSTYPE" == "darwin"* ]]; then
    # macOS
    exec bin/unix/check-deps.sh "$@"
elif [[ "$OSTYPE" == "linux-gnu"* ]]; then
    # Linux
    exec bin/unix/check-deps.sh "$@"
else
    # Default to Unix for other platforms
    exec bin/unix/check-deps.sh "$@"
fi
