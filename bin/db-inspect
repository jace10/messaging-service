#!/bin/bash

# Database inspection script for messaging service
# Supports Unix-like systems (Linux, macOS)

set -e

# Get the directory where this script is located
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Run the Unix database inspect script
exec "$SCRIPT_DIR/unix/db-inspect.sh" "$@"
